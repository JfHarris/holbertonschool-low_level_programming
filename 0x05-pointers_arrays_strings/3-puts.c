#include "main.h"
/**
 *_puts- print string, new line, to stdout
 *@str: pointer
 * Return: Always 0.
 */
void _puts(char *str)
{
int x;
 int y;
 
 for (x = 0 ; str[x] != ; x++)
   {
     x++;
   }
 
 _putchar('\n');
return;
}
