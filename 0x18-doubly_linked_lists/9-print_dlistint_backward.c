#include "lists.h"

/**
 * print_dlistint_backward - prints elements of list backwards
 *@h: pointer to head node
 * Return: elements and contents of node.
 */

size_t print_dlistint_backward(const dlistint_t *h __attribute__((unused)))
{
	printf("Oops\n");
	return (0);
}
